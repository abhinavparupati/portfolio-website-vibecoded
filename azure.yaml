# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: parupati-portfolio
metadata:
  template: parupati-portfolio@0.0.1-beta
infra:
  provider: bicep
  path: infra

services:
  frontend:
    project: ./SampleApp/FrontEnd
    language: dotnet
    host: appservice
